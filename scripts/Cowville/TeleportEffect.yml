TeleporBangtEffect:
  Type: World
  Events:
    On Player Teleports:
      - if <player.group[guardian]> {
        - narrate targets:NiggerBob "works"
        } else {
        - narrate targets:NiggerBob "Nope"
        }